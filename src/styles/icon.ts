import {StyleSheet} from 'react-native';

const icon = StyleSheet.create({
  size: {
    width: 32,
    height: 32,
  },
  profile: {
    width: 40,
    height: 40,
    borderRadius: 100,
  },
});

export default icon;
